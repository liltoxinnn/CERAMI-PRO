<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Typographie : lisible de loin, dans un atelier, sans lunettes. -->
    <FontFamily x:Key="PoliceInterface">Segoe UI</FontFamily>

    <Style x:Key="TitrePage" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="24" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Texte}" />
    </Style>

    <Style x:Key="IntroductionPage" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Foreground" Value="{StaticResource TexteSecondaire}" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <Style x:Key="TitreCarte" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="15" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource Texte}" />
        <Setter Property="Margin" Value="0,0,0,8" />
    </Style>

    <!-- Carte : le conteneur de base de tous les écrans. -->
    <Style x:Key="Carte" TargetType="Border">
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="BorderBrush" Value="{StaticResource Bordure}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="8" />
        <Setter Property="Padding" Value="20" />
        <Setter Property="Margin" Value="0,0,0,16" />
    </Style>

    <!-- Boutons -->
    <Style x:Key="BoutonBase" TargetType="Button">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Padding" Value="16,9" />
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="Cursor" Value="Hand" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border x:Name="Fond"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="Fond" Property="Opacity" Value="0.88" />
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="Fond" Property="Opacity" Value="0.45" />
                            <Setter Property="Cursor" Value="Arrow" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="BoutonPrincipal" TargetType="Button" BasedOn="{StaticResource BoutonBase}">
        <Setter Property="Background" Value="{StaticResource Terre}" />
        <Setter Property="BorderBrush" Value="{StaticResource Terre}" />
        <Setter Property="Foreground" Value="{StaticResource TexteInverse}" />
    </Style>

    <Style x:Key="BoutonSecondaire" TargetType="Button" BasedOn="{StaticResource BoutonBase}">
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="BorderBrush" Value="{StaticResource Bordure}" />
        <Setter Property="Foreground" Value="{StaticResource Texte}" />
    </Style>

    <Style x:Key="BoutonDanger" TargetType="Button" BasedOn="{StaticResource BoutonBase}">
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="BorderBrush" Value="{StaticResource Danger}" />
        <Setter Property="Foreground" Value="{StaticResource Danger}" />
    </Style>

    <!-- Champs de saisie : hauteur confortable, bordure nette. -->
    <Style x:Key="Saisie" TargetType="TextBox">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="MinHeight" Value="38" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="BorderBrush" Value="{StaticResource Bordure}" />
        <Setter Property="BorderThickness" Value="1" />
    </Style>

    <Style x:Key="EtiquetteChamp" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{StaticResource TexteSecondaire}" />
        <Setter Property="Margin" Value="0,0,0,4" />
    </Style>

    <!-- Tableaux de données -->
    <Style x:Key="TableauDonnees" TargetType="DataGrid">
        <Setter Property="FontFamily" Value="{StaticResource PoliceInterface}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="Background" Value="{StaticResource Surface}" />
        <Setter Property="BorderBrush" Value="{StaticResource Bordure}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="RowHeight" Value="40" />
        <Setter Property="AutoGenerateColumns" Value="False" />
        <Setter Property="CanUserAddRows" Value="False" />
        <Setter Property="CanUserDeleteRows" Value="False" />
        <Setter Property="IsReadOnly" Value="True" />
        <Setter Property="GridLinesVisibility" Value="Horizontal" />
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource Bordure}" />
        <Setter Property="HeadersVisibility" Value="Column" />
        <Setter Property="SelectionMode" Value="Single" />
    </Style>

</ResourceDictionary>
